[project]
name = "hermes-agent-task"
version = "1.1.0"
description = "Hermes background-task plugin with automatic reply context restoration"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
target-version = "py311"
line-length = 120
